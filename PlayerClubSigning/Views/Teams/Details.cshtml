@model PlayerClubSigning.Models.Team

@{
    ViewData["Title"] = "Check players";
}

<h1>Check players</h1>

<div>
    <h4>@Html.DisplayFor(model => model.Name)</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayName("Player(s)")
        </dt>
    </dl>
    <ul>
        @foreach(var Player in Model.Players)
         {
            <li>@Html.DisplayFor(teamPlayer => Player.Name)</li>
         }
    </ul>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.TeamId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
